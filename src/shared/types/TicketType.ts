export type TicketListProps = {
  title: string;
  text: string;
  textColor: string;
};

export type TicketButtonProps = {
  text: string;
  bgColor: string;
  textColor: string;
  onClick: () => void;
};
