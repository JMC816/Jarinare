export type SeatChangeButtonProps = {
  text: string;
  bgColor: string;
  textColor: string;
  onClick: () => void;
};

export type SeatProps = {
  bgColor: string;
  borderColor: string;
  onClick?: () => void;
};
